# Example .env file for local development and as reference for production variables
# Copy this to a file named .env and fill values (DO NOT commit your real .env to git)

# Django settings
DJANGO_SECRET_KEY=django-insecure-your-secret-key-here
# DEBUG: 1 (true) or 0 (false)
DEBUG=1
# Comma-separated list of allowed hosts
ALLOWED_HOSTS=127.0.0.1,localhost

# Database (optional): provide DATABASE_URL to use Postgres or other DB in production
# Example: postgres://user:password@hostname:5432/dbname
#DATABASE_URL=postgres://user:password@host:5432/dbname

# Static and media (if using a cloud storage provider, set credentials here)
# For S3 example (optional)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_STORAGE_BUCKET_NAME=
# AWS_S3_REGION_NAME=

# Other environment variables your deployment may require
